/**
 * Image exporter — high-level helper for exporting Figma frames
 * as reference images for visual comparison.
 *
 * Placeholder for Phase 2 implementation.
 */

export async function exportReferenceImage(
  _fileId: string,
  _nodeId: string,
): Promise<Buffer> {
  throw new Error('exportReferenceImage is not yet implemented.');
}
