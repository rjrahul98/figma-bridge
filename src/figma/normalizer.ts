/**
 * Figma node normalizer — transforms raw FigmaNode trees
 * into the framework-agnostic DesignNode representation.
 *
 * Placeholder for Phase 2 implementation.
 */

import type { FigmaNode } from '../types/figma.js';
import type { DesignNode } from '../types/design-node.js';

export function normalizeNode(_node: FigmaNode): DesignNode {
  throw new Error('normalizeNode is not yet implemented.');
}
