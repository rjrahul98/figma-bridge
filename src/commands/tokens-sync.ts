/**
 * `figma-bridge tokens sync` — re-sync tokens from tailwind.config.
 *
 * Placeholder for Phase 2 implementation.
 */

export async function tokensSyncCommand(): Promise<void> {
  throw new Error('tokens sync command is not yet implemented.');
}
